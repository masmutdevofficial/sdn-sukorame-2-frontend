import { ADMIN_ACCOUNT_KEY } from '~/composables/useAdminAccount'
export const useAuth=()=>{const loggedIn=useState('demo-auth',()=>false);onMounted(()=>loggedIn.value=sessionStorage.getItem('sdn-demo-auth')==='1');const login=(email:string,password:string)=>{let credentials={email:'admin@demo.local',password:'admin123'};try{credentials={...credentials,...JSON.parse(localStorage.getItem(ADMIN_ACCOUNT_KEY)||'null')}}catch{}if(email===credentials.email&&password===credentials.password){loggedIn.value=true;sessionStorage.setItem('sdn-demo-auth','1');return true}return false};const logout=()=>{loggedIn.value=false;sessionStorage.removeItem('sdn-demo-auth');return navigateTo('/admin/login')};return{loggedIn,login,logout}}
